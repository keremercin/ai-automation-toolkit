# Benchmark

## API Contract Quality
- Response envelope standardized across endpoints.
- Invalid payload path validated (`VALIDATION_ERROR`).
- Webhook fixtures included for support, lead, and document intake.
